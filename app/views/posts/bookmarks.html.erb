<div class="w-full px-4">
  <% if @bookmarks.present? %>
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
      <%= render partial: 'posts/post', collection: @bookmarks %>
    </div>
  <% else %>
    <div class="text-gray-500">ブックマークがありません</div>
  <% end %>
</div>
