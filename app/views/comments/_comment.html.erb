<!-- app/views/comments/_comment.html.erb -->

<div class="p-2 mb-3 border rounded font-bold">
  <p><strong><%= comment.user.name %></strong>：<%= comment.body %></p>

  <% if comment.user == current_user %>
    <%= link_to '編集', edit_post_comment_path(comment.post, comment), class: "text-blue-500 mr-2 hover:bg-gray-300 font-bold" %>

    <%= link_to '削除', post_comment_path(comment.post, comment),
                data: { turbo_method: :delete, turbo_confirm: '本当に削除しますか？' },
                class: "text-red-600 hover:bg-gray-300 font-bold" %>
  <% end %>
</div>
